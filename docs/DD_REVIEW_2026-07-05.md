# MY AI PET DD / PM / Design Review — 2026-07-05

## Agent Assignments

- DD manager: exchange listing / partner due diligence, token and payment claims, security posture, ownership controls.
- PM: MVP scope, live-vs-paused state, user and revenue evidence, operational readiness.
- Designer / UX: public copy, IA, mobile responsiveness, trust signals, accessibility.

## Fixed In This Pass

- Removed committed Grok API key fallbacks from image-generation scripts. The exposed key must still be revoked because it exists in git history.
- Restored Next build type gating by removing `ignoreBuildErrors` / unsupported build lint bypass from `web/next.config.ts`.
- Fixed TypeScript errors in wallet signature and write-contract paths by passing explicit wagmi accounts / target chains.
- Tightened USDT payment verification to require an exact ERC-20 `Transfer` match for token, sender, treasury, and amount instead of accepting the first token transfer with a 99% threshold.
- Broadened `web/.dockerignore` to exclude all `.env*` files from Docker build context.
- Updated `/contracts` ownership copy to disclose team-controlled paused operations and legacy PETToken/PETShop owner powers.
- Added a DB uniqueness invariant for reward redemptions so each user can claim each reward once.
- Upgraded Next.js / `eslint-config-next` to `16.2.10` and ran non-breaking `npm audit fix`.

## Remaining P0/P1 Blockers

- Economic truth source is still inconsistent across README, TODO, DD response, landing copy, docs, and contracts. Decide one official state: points-only live product, payments paused or enabled, chain target, token status, on-chain status.
- `docs/DD_RESPONSE.md` is not submission-ready. It still contains many `[TBD]` metrics for revenue, users, wallet counts, team, finance, geography, and treasury proofs.
- The exposed Grok key must be revoked/rotated outside the repo. Code deletion alone does not invalidate historical exposure.
- Token posture is unresolved. If `$PET` is abandoned, legacy PETToken/PETShop need formal deprecation evidence, pause/minter/owner proof, and docs cleanup. If retained, tokenomics, legal opinion, lockups, owner controls, and audit evidence are required.
- Multisig/timelock evidence is still missing for owner-level powers.
- Data deletion claims still need a full media/generation/provider-retention design. Current pet deletion does not clearly cover all generated media and external storage.
- PetSoul inheritance still needs product ownership transfer semantics, not only NFT owner metadata updates.
- Public partner/investor logo claims should be removed or backed by signed approval.
- Static landing has mobile overflow and stale crypto/token language. Either align it with the app or remove it from production routing.
- Full ESLint still fails due existing repo-wide `no-explicit-any` and React/Next lint debt; typecheck and build now pass.

## Verification Snapshot

- `web`: `npx tsc --noEmit` passes.
- `web`: `npm run build` passes on Next `16.2.10`.
- `contracts`: `npx hardhat compile` passes after clean `npm ci`.
- `contracts`: `npm audit --omit=dev --audit-level=high` reports 0 vulnerabilities.
- `web`: `npm audit --omit=dev --audit-level=high` still reports 1 high issue in the wagmi / WalletConnect transitive `ws` tree; available fix requires a breaking wagmi upgrade.
