# 플레이북 §2-⑤: 남용 방어용 존 정의. 사이트 conf의 비싼 location에
#   limit_req zone=abuse burst=10 nodelay;  를 추가해야 발동함.
# (sites-enabled/petclaw는 배포 도구가 관리하므로 여기엔 존만 정의)
limit_req_zone $binary_remote_addr zone=abuse:10m rate=2r/s;
limit_req_status 429;
