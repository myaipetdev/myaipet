// RainbowKit locale modules default-export a JSON string. This unreachable
// replacement keeps that contract while the app's provider remains English.
const translations = "{}";

export default translations;
